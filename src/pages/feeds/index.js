import SearchPanel from "../../components/pages/feeds/search-panel";

const Feeds = () => {
    return (
        <section>
            <SearchPanel />
        </section>
    );
};

export default Feeds;
